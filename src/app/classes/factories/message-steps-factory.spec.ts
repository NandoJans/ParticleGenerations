import { MessageStepsFactory } from './message-steps-factory';

describe('MessageStepsFactory', () => {
  it('should create an instance', () => {
    expect(new MessageStepsFactory()).toBeTruthy();
  });
});
