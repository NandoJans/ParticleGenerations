import { HoldingDisplayFactory } from './holding-display-factory';

describe('HoldingDisplayFactory', () => {
  it('should create an instance', () => {
    expect(new HoldingDisplayFactory()).toBeTruthy();
  });
});
