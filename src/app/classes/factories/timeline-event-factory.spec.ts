import { TimelineEventFactory } from './timeline-event-factory';

describe('TimelineEventFactory', () => {
  it('should create an instance', () => {
    const mockArg1 = {}; // Replace with appropriate mock or dummy value
    const mockArg2 = []; // Replace with appropriate mock or dummy value
    expect(new TimelineEventFactory(mockArg1, mockArg2)).toBeTruthy();
  });
});
