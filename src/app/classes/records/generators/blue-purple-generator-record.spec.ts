import { BluePurpleGeneratorRecord } from './blue-purple-generator-record';

describe('BluePurpleGeneratorRecord', () => {
  it('should create an instance', () => {
    expect(new BluePurpleGeneratorRecord()).toBeTruthy();
  });
});
