import { RedGeneratorRecord } from './red-generator-record';

describe('RedGeneratorRecord', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorRecord()).toBeTruthy();
  });
});
