import { YellowPurpleGeneratorRecord } from './yellow-purple-generator-record';

describe('YellowPurpleGeneratorRecord', () => {
  it('should create an instance', () => {
    expect(new YellowPurpleGeneratorRecord()).toBeTruthy();
  });
});
