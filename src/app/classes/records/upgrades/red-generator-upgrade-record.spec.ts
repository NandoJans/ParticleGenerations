import { RedGeneratorUpgradeRecord } from './red-generator-upgrade-record';

describe('RedGeneratorUpgradeRecord', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorUpgradeRecord()).toBeTruthy();
  });
});
