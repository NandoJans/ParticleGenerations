import { LimitedUpgradeRecord } from './limited-upgrade-record';

describe('LimitedUpgradeRecord', () => {
  it('should create an instance', () => {
    expect(new LimitedUpgradeRecord()).toBeTruthy();
  });
});
