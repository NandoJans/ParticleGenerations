import { YellowUpgradeRecord } from './yellow-upgrade-record';

describe('YellowUpgradeRecord', () => {
  it('should create an instance', () => {
    expect(new YellowUpgradeRecord()).toBeTruthy();
  });
});
