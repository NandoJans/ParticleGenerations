import { StartWithBoosterMilestone } from './start-with-booster-milestone';

describe('StartWithBoosterMilestone', () => {
  it('should create an instance', () => {
    expect(new StartWithBoosterMilestone()).toBeTruthy();
  });
});
