import { KeepRedUpgradesMilestone } from './keep-red-upgrades-milestone';

describe('KeepRedUpgradesMilestone', () => {
  it('should create an instance', () => {
    expect(new KeepRedUpgradesMilestone()).toBeTruthy();
  });
});
