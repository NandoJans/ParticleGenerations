import { BreakGreenBarrierMilestone } from './break-green-barrier-milestone';

describe('BreakGreenBarrierMilestone', () => {
  it('should create an instance', () => {
    expect(new BreakGreenBarrierMilestone()).toBeTruthy();
  });
});
