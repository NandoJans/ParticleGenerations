import { RedGeneratorExtensionAutomator } from './red-generator-extension-automator';

describe('RedGeneratorExtensionAutomator', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorExtensionAutomator()).toBeTruthy();
  });
});
