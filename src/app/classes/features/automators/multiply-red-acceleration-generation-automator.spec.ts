import { MultiplyRedAccelerationGenerationAutomator } from './multiply-red-acceleration-generation-automator';

describe('MultiplyRedAccelerationGenerationAutomator', () => {
  it('should create an instance', () => {
    expect(new MultiplyRedAccelerationGenerationAutomator()).toBeTruthy();
  });
});
