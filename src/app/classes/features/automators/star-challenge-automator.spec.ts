import { StarChallengeAutomator } from './star-challenge-automator';

describe('StarChallengeAutomator', () => {
  it('should create an instance', () => {
    expect(new StarChallengeAutomator()).toBeTruthy();
  });
});
