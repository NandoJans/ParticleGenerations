import { MultiplyRedAcceleratorEffectAutomator } from './multiply-red-accelerator-effect-automator';

describe('MultiplyRedAcceleratorEffectAutomator', () => {
  it('should create an instance', () => {
    expect(new MultiplyRedAcceleratorEffectAutomator()).toBeTruthy();
  });
});
