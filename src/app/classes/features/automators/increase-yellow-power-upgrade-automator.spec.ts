import { IncreaseYellowPowerUpgradeAutomator } from './increase-yellow-power-upgrade-automator';

describe('IncreaseYellowPowerUpgradeAutomator', () => {
  it('should create an instance', () => {
    expect(new IncreaseYellowPowerUpgradeAutomator()).toBeTruthy();
  });
});
