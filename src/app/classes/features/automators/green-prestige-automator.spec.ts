import { GreenPrestigeAutomator } from './green-prestige-automator';

describe('GreenPrestigeAutomator', () => {
  it('should create an instance', () => {
    expect(new GreenPrestigeAutomator('test')).toBeTruthy();
  });
});
