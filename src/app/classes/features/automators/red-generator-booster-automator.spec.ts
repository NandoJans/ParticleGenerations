import { RedGeneratorBoosterAutomator } from './red-generator-booster-automator';

describe('RedGeneratorBoosterAutomator', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorBoosterAutomator()).toBeTruthy();
  });
});
