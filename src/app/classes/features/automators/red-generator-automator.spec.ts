import { RedGeneratorAutomator } from './red-generator-automator';

describe('RedGeneratorAutomator', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorAutomator()).toBeTruthy();
  });
});
