import { ChallengeAutomator } from './challenge-automator';

describe('ChallengeAutomator', () => {
  it('should create an instance', () => {
    expect(new ChallengeAutomator()).toBeTruthy();
  });
});
