import { BoosterAccelerationAutomator } from './booster-acceleration-automator';

describe('BoosterAccelerationAutomator', () => {
  it('should create an instance', () => {
    expect(new BoosterAccelerationAutomator()).toBeTruthy();
  });
});
