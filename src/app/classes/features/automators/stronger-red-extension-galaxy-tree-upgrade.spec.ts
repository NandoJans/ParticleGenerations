import { StrongerRedExtensionGalaxyTreeUpgrade } from './stronger-red-extension-galaxy-tree-upgrade';

describe('StrongerRedExtensionGalaxyTreeUpgrade', () => {
  it('should create an instance', () => {
    expect(new StrongerRedExtensionGalaxyTreeUpgrade()).toBeTruthy();
  });
});
