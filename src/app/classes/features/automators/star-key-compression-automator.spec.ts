import { StarKeyCompressionAutomator } from './star-key-compression-automator';

describe('StarKeyCompressionAutomator', () => {
  it('should create an instance', () => {
    expect(new StarKeyCompressionAutomator()).toBeTruthy();
  });
});
