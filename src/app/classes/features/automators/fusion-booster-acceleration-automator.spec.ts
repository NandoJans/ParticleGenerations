import { FusionBoosterAccelerationAutomator } from './fusion-booster-acceleration-automator';

describe('FusionBoosterAccelerationAutomator', () => {
  it('should create an instance', () => {
    expect(new FusionBoosterAccelerationAutomator()).toBeTruthy();
  });
});
