import { RedGeneratorDarkStarCharger } from './red-generator-dark-star-charger';

describe('RedGeneratorDarkCharger', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorDarkStarCharger()).toBeTruthy();
  });
});
