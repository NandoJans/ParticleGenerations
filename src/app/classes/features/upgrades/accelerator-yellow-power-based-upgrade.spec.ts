import { AcceleratorYellowPowerBasedUpgrade } from './accelerator-yellow-power-based-upgrade';

describe('AcceleratorYellowPowerBasedUpgrade', () => {
  it('should create an instance', () => {
    expect(new AcceleratorYellowPowerBasedUpgrade()).toBeTruthy();
  });
});
