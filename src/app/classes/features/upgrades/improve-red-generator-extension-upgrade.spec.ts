import { ImproveRedGeneratorExtensionUpgrade } from './improve-red-generator-extension-upgrade';

describe('ImproveRedGeneratorExtensionUpgrade', () => {
  it('should create an instance', () => {
    expect(new ImproveRedGeneratorExtensionUpgrade()).toBeTruthy();
  });
});
