import { BetterNuclearDecayUpgrade } from './better-nuclear-decay-upgrade';

describe('BetterNuclearDecayUpgrade', () => {
  it('should create an instance', () => {
    expect(new BetterNuclearDecayUpgrade()).toBeTruthy();
  });
});
