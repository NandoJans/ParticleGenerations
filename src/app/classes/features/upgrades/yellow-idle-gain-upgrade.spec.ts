import { YellowIdleGainUpgrade } from './yellow-idle-gain-upgrade';

describe('YellowIdleGainUpgrade', () => {
  it('should create an instance', () => {
    expect(new YellowIdleGainUpgrade()).toBeTruthy();
  });
});
