import { AcceleratedCompressionGalaxyTreeUpgrade } from './accelerated-compression-galaxy-tree-upgrade';

describe('AcceleratedCompressionGalaxyTreeUpgrade', () => {
  it('should create an instance', () => {
    expect(new AcceleratedCompressionGalaxyTreeUpgrade()).toBeTruthy();
  });
});
