import { RedGeneratorMultiplierUpgrade } from './red-generator-multiplier-upgrade';

describe('RedGeneratorMultiplierUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorMultiplierUpgrade()).toBeTruthy();
  });
});
