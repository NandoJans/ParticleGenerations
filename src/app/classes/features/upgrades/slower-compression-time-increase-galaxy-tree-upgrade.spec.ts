import { SlowerCompressionTimeIncreaseGalaxyTreeUpgrade } from './slower-compression-time-increase-galaxy-tree-upgrade';

describe('SlowerCompressionTimeIncreaseGalaxyTreeUpgrade', () => {
  it('should create an instance', () => {
    expect(new SlowerCompressionTimeIncreaseGalaxyTreeUpgrade()).toBeTruthy();
  });
});
