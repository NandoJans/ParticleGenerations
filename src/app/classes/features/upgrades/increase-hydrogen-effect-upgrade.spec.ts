import { IncreaseHydrogenEffectUpgrade } from './increase-hydrogen-effect-upgrade';

describe('IncreaseHydrogenEffectUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseHydrogenEffectUpgrade()).toBeTruthy();
  });
});
