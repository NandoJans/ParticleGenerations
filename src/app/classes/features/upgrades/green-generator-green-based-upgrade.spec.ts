import { GreenGeneratorsGreenBasedUpgrade } from './green-generators-green-based-upgrade';

describe('GreenGeneratorGreenBasedUpgrade', () => {
  it('should create an instance', () => {
    expect(new GreenGeneratorsGreenBasedUpgrade()).toBeTruthy();
  });
});
