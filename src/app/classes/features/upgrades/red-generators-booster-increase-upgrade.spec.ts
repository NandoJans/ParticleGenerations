import { RedGeneratorsBoosterIncreaseUpgrade } from './red-generators-booster-increase-upgrade';

describe('RedGeneratorsBoosterIncreaseUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorsBoosterIncreaseUpgrade()).toBeTruthy();
  });
});
