import { DarkRedGeneratorsUpgrade } from './dark-red-generators-upgrade';

describe('DarkRedGeneratorsUpgrade', () => {
  it('should create an instance', () => {
    expect(new DarkRedGeneratorsUpgrade()).toBeTruthy();
  });
});
