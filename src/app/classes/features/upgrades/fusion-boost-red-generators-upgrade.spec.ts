import { FusionBoostRedGeneratorsUpgrade } from './fusion-boost-red-generators-upgrade';

describe('FusionBoostRedGeneratorsUpgrade', () => {
  it('should create an instance', () => {
    expect(new FusionBoostRedGeneratorsUpgrade()).toBeTruthy();
  });
});
