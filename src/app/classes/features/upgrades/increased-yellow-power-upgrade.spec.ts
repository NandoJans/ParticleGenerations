import { IncreasedYellowPowerUpgrade } from './increased-yellow-power-upgrade';

describe('IncreasedYellowPowerUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreasedYellowPowerUpgrade()).toBeTruthy();
  });
});
