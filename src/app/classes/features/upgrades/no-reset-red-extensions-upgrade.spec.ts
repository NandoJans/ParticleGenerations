import { NoResetRedExtensionsUpgrade } from './no-reset-red-extensions-upgrade';

describe('NoResetRedExtensionsUpgrade', () => {
  it('should create an instance', () => {
    expect(new NoResetRedExtensionsUpgrade()).toBeTruthy();
  });
});
