import { IncreaseStarGravityUpgrade } from './increase-star-gravity-upgrade';

describe('IncreaseStarGravityUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseStarGravityUpgrade()).toBeTruthy();
  });
});
