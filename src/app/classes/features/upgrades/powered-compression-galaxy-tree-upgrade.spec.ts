import { PoweredCompressionGalaxyTreeUpgrade } from './powered-compression-galaxy-tree-upgrade';

describe('PoweredCompressionGalaxyTreeUpgrade', () => {
  it('should create an instance', () => {
    expect(new PoweredCompressionGalaxyTreeUpgrade()).toBeTruthy();
  });
});
