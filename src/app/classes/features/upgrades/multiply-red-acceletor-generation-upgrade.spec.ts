import { MultiplyRedAcceletorGenerationUpgrade } from './multiply-red-acceletor-generation-upgrade';

describe('MultiplyRedAcceletorGenerationUpgrade', () => {
  it('should create an instance', () => {
    expect(new MultiplyRedAcceletorGenerationUpgrade()).toBeTruthy();
  });
});
