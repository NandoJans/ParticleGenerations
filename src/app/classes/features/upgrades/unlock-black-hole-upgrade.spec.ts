import { UnlockBlackHoleUpgrade } from './unlock-black-hole-upgrade';

describe('UnlockBlackHoleUpgrade', () => {
  it('should create an instance', () => {
    expect(new UnlockBlackHoleUpgrade()).toBeTruthy();
  });
});
