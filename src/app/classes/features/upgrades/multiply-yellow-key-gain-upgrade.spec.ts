import { MultiplyYellowKeyGainUpgrade } from './multiply-yellow-key-gain-upgrade';

describe('MultiplyYellowKeyGainUpgrade', () => {
  it('should create an instance', () => {
    expect(new MultiplyYellowKeyGainUpgrade()).toBeTruthy();
  });
});
