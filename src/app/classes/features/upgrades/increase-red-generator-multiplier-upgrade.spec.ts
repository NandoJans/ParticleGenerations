import { IncreaseRedGeneratorMultiplierUpgrade } from './increase-red-generator-multiplier-upgrade';

describe('IncreaseRedGeneratorMultiplierUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseRedGeneratorMultiplierUpgrade()).toBeTruthy();
  });
});
