import { RedGeneratorUpgrade } from './red-generator-upgrade';

describe('RedGeneratorUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorUpgrade()).toBeTruthy();
  });
});
