import { YellowRepeatableMultiplierUpgrade } from './yellow-repeatable-multiplier-upgrade';

describe('YellowRepeatableMultiplierUpgrade', () => {
  it('should create an instance', () => {
    expect(new YellowRepeatableMultiplierUpgrade()).toBeTruthy();
  });
});
