import { GreenGeneratorUpgrade } from './green-generator-upgrade';

describe('GreenGeneratorUpgrade', () => {
  it('should create an instance', () => {
    expect(new GreenGeneratorUpgrade()).toBeTruthy();
  });
});
