import { GreenGeneratorCostDivisorUpgrade } from './green-generator-cost-divisor-upgrade';

describe('GreenGeneratorCostDivisorUpgrade', () => {
  it('should create an instance', () => {
    expect(new GreenGeneratorCostDivisorUpgrade()).toBeTruthy();
  });
});
