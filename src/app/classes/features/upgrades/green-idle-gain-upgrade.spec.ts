import { GreenIdleGainUpgrade } from './green-idle-gain-upgrade';

describe('GreenIdleGainUpgrade', () => {
  it('should create an instance', () => {
    expect(new GreenIdleGainUpgrade()).toBeTruthy();
  });
});
