import { RedPurpleBufferUpgrade } from './red-purple-buffer-upgrade';

describe('RedPurpleBufferUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedPurpleBufferUpgrade()).toBeTruthy();
  });
});
