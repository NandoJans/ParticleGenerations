import { HydrogenCompressionGalaxyTreeUpgrade } from './hydrogen-compression-galaxy-tree-upgrade';

describe('HydrogenCompressionGalaxyTreeUpgrade', () => {
  it('should create an instance', () => {
    expect(new HydrogenCompressionGalaxyTreeUpgrade()).toBeTruthy();
  });
});
