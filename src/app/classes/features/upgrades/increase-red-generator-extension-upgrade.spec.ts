import { IncreaseRedGeneratorExtensionUpgrade } from './increase-red-generator-extension-upgrade';

describe('IncreaseRedGeneratorExtensionUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseRedGeneratorExtensionUpgrade()).toBeTruthy();
  });
});
