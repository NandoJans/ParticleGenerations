import { SetHoldingUpgrade } from './set-holding-upgrade';

describe('SetHoldingUpgrade', () => {
  it('should create an instance', () => {
    expect(new SetHoldingUpgrade()).toBeTruthy();
  });
});
