import { RedAcceleratorStartGalaxyTreeUpgrade } from './red-accelerator-start-galaxy-tree-upgrade';

describe('RedAcceleratorStartGalaxyTreeUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedAcceleratorStartGalaxyTreeUpgrade()).toBeTruthy();
  });
});
