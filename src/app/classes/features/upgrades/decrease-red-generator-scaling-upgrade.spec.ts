import { DecreaseRedGeneratorScalingUpgrade } from './decrease-red-generator-scaling-upgrade';

describe('DecreaseRedGeneratorScalingUpgrade', () => {
  it('should create an instance', () => {
    expect(new DecreaseRedGeneratorScalingUpgrade()).toBeTruthy();
  });
});
