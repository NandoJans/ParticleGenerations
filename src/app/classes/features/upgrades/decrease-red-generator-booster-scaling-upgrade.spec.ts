import { DecreaseRedGeneratorBoosterScalingUpgrade } from './decrease-red-generator-booster-scaling-upgrade';

describe('DecreaseRedGeneratorBoosterScalingUpgrade', () => {
  it('should create an instance', () => {
    expect(new DecreaseRedGeneratorBoosterScalingUpgrade()).toBeTruthy();
  });
});
