import { BluePurpleBufferUpgrade } from './blue-purple-buffer-upgrade';

describe('BluePurpleBufferUpgrade', () => {
  it('should create an instance', () => {
    expect(new BluePurpleBufferUpgrade()).toBeTruthy();
  });
});
