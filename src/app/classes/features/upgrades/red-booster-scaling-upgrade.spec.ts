import { RedBoosterScalingUpgrade } from './red-booster-scaling-upgrade';

describe('RedBoosterScalingUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedBoosterScalingUpgrade()).toBeTruthy();
  });
});
