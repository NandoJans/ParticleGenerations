import { UnlockRedGeneratorBoosterUpgrade } from './unlock-red-generator-booster-upgrade';

describe('UnlockRedGeneratorBoosterUpgrade', () => {
  it('should create an instance', () => {
    expect(new UnlockRedGeneratorBoosterUpgrade()).toBeTruthy();
  });
});
