import { IncreaseSiriusGoalUpgrade } from './increase-sirius-goal-upgrade';

describe('IncreaseSiriusGoalUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseSiriusGoalUpgrade()).toBeTruthy();
  });
});
