import { NuclearDecayUpgrade } from './nuclear-decay-upgrade';

describe('NuclearDecayUpgrade', () => {
  it('should create an instance', () => {
    expect(new NuclearDecayUpgrade()).toBeTruthy();
  });
});
