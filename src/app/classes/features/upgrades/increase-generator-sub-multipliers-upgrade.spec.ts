import { IncreaseRedGeneratorSubMultipliersUpgrade } from './increase-red-generator-sub-multipliers-upgrade';

describe('IncreaseGeneratorSubMultipliersUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseRedGeneratorSubMultipliersUpgrade()).toBeTruthy();
  });
});
