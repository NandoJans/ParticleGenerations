import { IncreaseProximaCentauriGoalUpgrade } from './increase-proxima-centauri-goal-upgrade';

describe('IncreaseProximaCentauriGoalUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseProximaCentauriGoalUpgrade()).toBeTruthy();
  });
});
