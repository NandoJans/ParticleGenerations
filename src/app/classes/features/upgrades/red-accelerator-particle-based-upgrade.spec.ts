import { RedAcceleratorParticleBasedUpgrade } from './red-accelerator-particle-based-upgrade';

describe('RedAcceleratorParticleBasedUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedAcceleratorParticleBasedUpgrade()).toBeTruthy();
  });
});
