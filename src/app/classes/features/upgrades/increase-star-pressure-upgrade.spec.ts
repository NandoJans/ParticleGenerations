import { IncreaseStarPressureUpgrade } from './increase-star-pressure-upgrade';

describe('IncreaseStarPressureUpgrade', () => {
  it('should create an instance', () => {
    expect(new IncreaseStarPressureUpgrade()).toBeTruthy();
  });
});
