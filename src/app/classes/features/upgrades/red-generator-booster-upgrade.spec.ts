import { RedGeneratorBoosterUpgrade } from './red-generator-booster-upgrade';

describe('RedGeneratorBoosterUpgrade', () => {
  it('should create an instance', () => {
    expect(new RedGeneratorBoosterUpgrade()).toBeTruthy();
  });
});
