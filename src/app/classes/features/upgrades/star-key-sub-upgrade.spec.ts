import { StarKeySubUpgrade } from './star-key-sub-upgrade';

describe('StarKeySubUpgrade', () => {
  it('should create an instance', () => {
    expect(new StarKeySubUpgrade()).toBeTruthy();
  });
});
