import { RedParticleHolding } from './red-particle-holding';

describe('RedParticleHolding', () => {
  it('should create an instance', () => {
    expect(new RedParticleHolding()).toBeTruthy();
  });
});
