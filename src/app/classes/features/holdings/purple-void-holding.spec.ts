import { PurpleVoidHolding } from './purple-void-holding';

describe('PurpleVoidHolding', () => {
  it('should create an instance', () => {
    expect(new PurpleVoidHolding()).toBeTruthy();
  });
});
