import { LalandeStarChallenge } from './lalande-star-challenge';

describe('LalandeStarChallenge', () => {
  it('should create an instance', () => {
    expect(new LalandeStarChallenge()).toBeTruthy();
  });
});
