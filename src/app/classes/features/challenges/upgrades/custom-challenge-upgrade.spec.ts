import { CustomChallengeUpgrade } from './custom-challenge-upgrade';

describe('CustomChallengeUpgrade', () => {
  it('should create an instance', () => {
    expect(new CustomChallengeUpgrade()).toBeTruthy();
  });
});
