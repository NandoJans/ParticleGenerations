import { FirstYellowPurpleGenerator } from './first-yellow-purple-generator';

describe('FirstYellowPurpleGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstYellowPurpleGenerator()).toBeTruthy();
  });
});
