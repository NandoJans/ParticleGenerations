import { FifthRedGenerator } from './fifth-red-generator';

describe('FifthRedGenerator', () => {
  it('should create an instance', () => {
    expect(new FifthRedGenerator()).toBeTruthy();
  });
});
