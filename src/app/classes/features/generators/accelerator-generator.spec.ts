import { AcceleratorGenerator } from './accelerator-generator';

describe('AcceleratorGenerator', () => {
  it('should create an instance', () => {
    expect(new AcceleratorGenerator()).toBeTruthy();
  });
});
