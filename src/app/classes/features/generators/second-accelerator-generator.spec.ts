import { SecondAcceleratorGenerator } from './second-accelerator-generator';

describe('SecondAcceleratorGenerator', () => {
  it('should create an instance', () => {
    expect(new SecondAcceleratorGenerator()).toBeTruthy();
  });
});
