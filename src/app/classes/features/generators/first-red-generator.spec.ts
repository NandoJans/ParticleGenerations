import { FirstRedGenerator } from './first-red-generator';

describe('FirstRedGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstRedGenerator()).toBeTruthy();
  });
});
