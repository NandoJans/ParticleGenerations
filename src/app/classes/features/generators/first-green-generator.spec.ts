import { FirstGreenGenerator } from './first-green-generator';

describe('FirstGreenGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstGreenGenerator()).toBeTruthy();
  });
});
