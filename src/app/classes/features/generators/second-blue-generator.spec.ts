import { SecondBlueGenerator } from './second-blue-generator';

describe('SecondBlueGenerator', () => {
  it('should create an instance', () => {
    expect(new SecondBlueGenerator()).toBeTruthy();
  });
});
