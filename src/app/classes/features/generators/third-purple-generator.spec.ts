import { ThirdPurpleGenerator } from './third-purple-generator';

describe('ThirdPurpleGenerator', () => {
  it('should create an instance', () => {
    expect(new ThirdPurpleGenerator()).toBeTruthy();
  });
});
