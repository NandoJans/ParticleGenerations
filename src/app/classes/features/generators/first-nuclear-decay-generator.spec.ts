import { FirstNuclearDecayGenerator } from './first-nuclear-decay-generator';

describe('FirstNuclearDecayGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstNuclearDecayGenerator()).toBeTruthy();
  });
});
