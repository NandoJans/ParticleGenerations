import { FirstPurpleGenerator } from './first-purple-generator';

describe('FirstPurpleGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstPurpleGenerator()).toBeTruthy();
  });
});
