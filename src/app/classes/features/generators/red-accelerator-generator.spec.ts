import { RedAcceleratorGenerator } from './red-accelerator-generator';

describe('RedAcceleratorGenerator', () => {
  it('should create an instance', () => {
    expect(new RedAcceleratorGenerator()).toBeTruthy();
  });
});
