import { FirstBluePurpleGenerator } from './first-blue-purple-generator';

describe('FirstBluePurpleGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstBluePurpleGenerator()).toBeTruthy();
  });
});
