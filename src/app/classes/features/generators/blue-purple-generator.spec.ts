import { BluePurpleGenerator } from './blue-purple-generator';

describe('BluePurpleGenerator', () => {
  it('should create an instance', () => {
    expect(new BluePurpleGenerator()).toBeTruthy();
  });
});
