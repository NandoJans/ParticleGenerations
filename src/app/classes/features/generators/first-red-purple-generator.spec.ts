import { FirstRedPurpleGenerator } from './first-red-purple-generator';

describe('FirstRedPurpleGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstRedPurpleGenerator()).toBeTruthy();
  });
});
