import { ThirdNuclearDecayGenerator } from './third-nuclear-decay-generator';

describe('ThirdNuclearGenerator', () => {
  it('should create an instance', () => {
    expect(new ThirdNuclearDecayGenerator()).toBeTruthy();
  });
});
