import { ThirdGreenGenerator } from './third-green-generator';

describe('ThirdGreenGenerator', () => {
  it('should create an instance', () => {
    expect(new ThirdGreenGenerator()).toBeTruthy();
  });
});
