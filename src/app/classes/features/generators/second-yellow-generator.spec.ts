import { SecondYellowGenerator } from './second-yellow-generator';

describe('SecondYellowGenerator', () => {
  it('should create an instance', () => {
    expect(new SecondYellowGenerator()).toBeTruthy();
  });
});
