import { FirstAcceleratorGenerator } from './first-accelerator-generator';

describe('FirstAcceleratorGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstAcceleratorGenerator()).toBeTruthy();
  });
});
