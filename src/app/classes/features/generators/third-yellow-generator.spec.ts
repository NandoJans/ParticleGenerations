import { ThirdYellowGenerator } from './third-yellow-generator';

describe('ThirdYellowGenerator', () => {
  it('should create an instance', () => {
    expect(new ThirdYellowGenerator()).toBeTruthy();
  });
});
