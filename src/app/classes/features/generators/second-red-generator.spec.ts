import { SecondRedGenerator } from './second-red-generator';

describe('SecondRedGenerator', () => {
  it('should create an instance', () => {
    expect(new SecondRedGenerator()).toBeTruthy();
  });
});
