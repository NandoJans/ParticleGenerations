import { FirstBlueNeutronGenerator } from './first-blue-neutron-generator';

describe('FirstBlueNeutronGenerator', () => {
  it('should create an instance', () => {
    expect(new FirstBlueNeutronGenerator()).toBeTruthy();
  });
});
