import { GameElementHelper } from './game-element-helper';

describe('GameElementHelper', () => {
  it('should create an instance', () => {
    expect(new GameElementHelper()).toBeTruthy();
  });
});
