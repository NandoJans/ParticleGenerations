import { MigrateNumAndExpValues } from './migrate-num-and-exp-values';

describe('MigrateNumAndExpValues', () => {
  it('should create an instance', () => {
    expect(new MigrateNumAndExpValues()).toBeTruthy();
  });
});
