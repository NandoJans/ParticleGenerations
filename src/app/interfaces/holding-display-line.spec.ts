import { HoldingDisplayLine } from './holding-display-line';

describe('HoldingDisplayLine', () => {
  it('should create an instance', () => {
    expect(new HoldingDisplayLine()).toBeTruthy();
  });
});
